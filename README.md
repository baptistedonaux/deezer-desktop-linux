# How to run

```
npm install
npm run pepper
npm start
```

# How to build

> /!\ Currently this part doesn't work.

```
npm install
cd app && npm install && cd ..
npm run pepper
npm run build
npm run dist
```